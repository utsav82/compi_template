
            // if(count>n)
            // {
            //     out.println(-1);return;
            // }
            // while(count-->0)
            // {
            //     list.add(k-1);
            // }
            // while (list.size() != n) {
            //     list.add((long) 0);
            // }

            // long sum = 0;
            // long bcheck = 0;
            // for (int i = 0; i < n; i++) {
            //     sum += list.get(i);
            //     bcheck += list.get(i) / k;
            // }

            // if (bcheck == b && sum == sumcopy) {

            //     out.println(list.toArray());
            // } else {
            //     out.println(-1);
            // }